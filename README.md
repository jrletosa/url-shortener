Web application to shorten long URLs. The project is written in Python and makes use of the Django framework.

Running instance
----------------
A running instance of the web service can be found at:

    http://jrl-shorturl.herokuapp.com/